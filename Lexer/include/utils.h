#ifndef UTILS_H
#define UTILS_H

bool isAlpha(char);
bool isOctalDigit(char);
bool isDecimalDigit(char);
bool isHexadecDigit(char);
bool isAlnum(char);
bool isWhitespace(char);
bool isValidNameChar(char);

char parseHexDigit(char);

#endif